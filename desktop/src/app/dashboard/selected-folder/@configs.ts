import { RouteConfig } from "@/common/@types";
import Constants from "@/utils/constants";

export const routeConfigs = {
  selectedFolder: new RouteConfig({
    route: Constants.PAGES.SELECTED_FOLDER,
  }),
};
